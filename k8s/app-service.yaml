apiVersion: v1
kind: Service
metadata:
  name: vab-app-service
spec:
  type: NodePort
  selector:
    app: vab-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
