@{
    ViewData["Title"] = "Espace Agent — VAB";
}
<div class="container mt-5">
    <div class="card border-primary shadow">
        <div class="card-header bg-primary text-white py-3">
            <h5 class="mb-0">✈ Espace Saisie Agent — VAB</h5>
        </div>
        <div class="card-body">
            <p class="lead">Bienvenue dans l'espace de saisie des états des ventes et des offres à bord.</p>
            <p class="text-muted">Sélectionnez une action ci-dessous :</p>
            <hr />
            <div class="d-grid gap-3 d-md-flex">
                <a href="#" class="btn btn-primary fw-bold">Saisir état des ventes</a>
                <a href="#" class="btn btn-outline-secondary fw-bold">Saisir offres à bord</a>
            </div>
        </div>
    </div>
</div>
