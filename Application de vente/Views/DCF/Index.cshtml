@{
    ViewData["Title"] = "Espace DCF — VAB";
}
<div class="container mt-5">
    <div class="card border-warning shadow">
        <div class="card-header bg-warning text-dark py-3">
            <h5 class="mb-0">🏛 Espace Direction Centrale Financière (DCF)</h5>
        </div>
        <div class="card-body">
            <p class="lead">Bienvenue dans l'espace de validation des factures et d'archivage des documents.</p>
            <p class="text-muted">Sélectionnez une action ci-dessous :</p>
            <hr />
            <div class="d-grid gap-3 d-md-flex">
                <a href="#" class="btn btn-warning fw-bold text-dark">Valider Factures</a>
                <a href="#" class="btn btn-outline-secondary fw-bold">Consulter Archivage</a>
            </div>
        </div>
    </div>
</div>
